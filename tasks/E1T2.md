# E1T2 — Time Slot Availability Display

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Compute and display available time slots for the selected date. Time slots are 15-minute intervals, filtered to avoid double-bookings for the 90-minute duration. Show available slots as inline buttons. Write the dialog spec to tests/specs/E1T2.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E1T2.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

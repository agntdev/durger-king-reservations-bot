# E1T5 — Booking Confirmation and Inline Actions

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Assign the smallest single table that fits the party size and create the booking. Show a confirmation message with a summary, booking ID, and inline buttons for 'Cancel booking', 'Reschedule', and 'My bookings'. Write the dialog spec to tests/specs/E1T5.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E1T5.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

# E1T4 — Guest Contact Collection

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Collect guest name and phone (via Telegram contact sharing or manual input). Automatically record the guest's Telegram ID. Write the dialog spec to tests/specs/E1T4.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-bot-sessions, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E1T4.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

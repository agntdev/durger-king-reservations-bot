# E4T1 — Admin Setup and Configuration

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Allow the first admin to configure the owner Telegram ID, timezone, and booking window. Write the dialog spec to tests/specs/E4T1.json and append the /admin command to tests/commands.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E4T1.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

# E7T2 — User Experience Improvements

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Improve the user experience by providing clear instructions, feedback, and a smooth flow through the reservation process. Write the dialog spec to tests/specs/E7T2.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E7T2.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

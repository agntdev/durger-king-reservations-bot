# E2T2 — No-Show Detection and Owner Notification

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

If a booking is not marked 'Arrived' within 30 minutes after the scheduled start, mark it as 'no-show' and notify the owner. This requires a scheduled job to check for unarrived bookings. Write the dialog spec to tests/specs/E2T2.json.

## Skills

**Skills:** telegram-bot-basics, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E2T2.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

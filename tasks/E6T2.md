# E6T2 — Secure Admin Access and Configuration

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Implement secure admin access by ensuring only the configured owner Telegram ID can access admin commands and settings. Write the dialog spec to tests/specs/E6T2.json.

## Skills

**Skills:** telegram-bot-basics, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E6T2.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

# E5T2 — Redis Job Queue for Reminders and No-Show Detection

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Implement Redis job queue for scheduling reminders and no-show detection. This includes setting up delayed jobs for 2-hour reminders and 30-minute no-show checks. Write the dialog spec to tests/specs/E5T2.json.

## Skills

**Skills:** telegram-bot-basics, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E5T2.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

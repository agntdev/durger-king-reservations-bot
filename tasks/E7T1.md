# E7T1 — Error Handling and Recovery

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Implement error handling for invalid inputs, unexpected states, and system errors. Provide clear error messages and recovery options to the user. Write the dialog spec to tests/specs/E7T1.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E7T1.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

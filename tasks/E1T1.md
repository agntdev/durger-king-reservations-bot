# E1T1 — Date Selection with Inline Calendar

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Implement an inline calendar for guests to select a reservation date. The calendar should show available dates up to the booking window and handle date selection via callback queries. Write the dialog spec to tests/specs/E1T1.json and append the /reserve command to tests/commands.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-bot-sessions, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E1T1.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

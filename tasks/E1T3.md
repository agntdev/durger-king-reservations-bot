# E1T3 — Party Size Validation

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

After selecting a time slot, prompt the guest for party size (1–4). Validate that the selected time slot can accommodate the requested party size based on table inventory. Write the dialog spec to tests/specs/E1T3.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E1T3.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

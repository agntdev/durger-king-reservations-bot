# E4T2 — Admin Booking Management Commands

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Implement admin commands to view and manage bookings: /bookings_today, /bookings_date YYYY-MM-DD, /capacity_today, /no_shows, and quick actions to mark 'Arrived', 'Cancel', or 'Reschedule'. Write the dialog spec to tests/specs/E4T2.json.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E4T2.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.

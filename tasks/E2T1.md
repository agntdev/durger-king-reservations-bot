# E2T1 — Guest Reminder 2 Hours Before Booking

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 DKB

Send an automated reminder message to the guest 2 hours before their reservation. This requires scheduling a job in Redis or using the internal scheduler. Write the dialog spec to tests/specs/E2T1.json.

## Skills

**Skills:** telegram-bot-basics, telegram-test-specs

Read `tasks/_skills/<name>.md` for each skill above before implementing.


## Dialog tests

If this task adds or changes user-facing bot behavior, author its dialog tests as a `BotSpec` JSON array in its OWN file `tests/specs/E2T1.json`. NEVER edit or append to a shared `tests/specs.json` — concurrent feature PRs would conflict on it. The tests-gate globs and merges all `tests/specs/*.json`.
